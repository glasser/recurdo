PATH_add bin

# A gitignored file (eg, put API_KEY here)
[[ -f .envrc.local ]] && dotenv ".envrc.local"

# Select Java and Gradle using SDKMAN!
# See https://github.com/direnv/direnv/issues/420 for use_sdk in ~/.direnvrc
use sdk java 8.0.252-zulu gradle 6.4.1
